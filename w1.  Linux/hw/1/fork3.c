#include <unistd.h>
int main()
{
    int i; // счетчик цикла
    for (i = 0; i < 3; i++) // цикл с начальным значением 1, шагом 1 и конечным значением 3.
    {
       	int pid=fork();
    }
    usleep(5000000);
    return 0;
 }

